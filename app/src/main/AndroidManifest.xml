<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.a3dprint_control">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".PrintersAdmin"></activity>
        <activity android:name=".Impresion" />
        <activity android:name=".Consulta" />
        <activity android:name=".Printer2" />
        <activity android:name=".Printer1" />
        <activity android:name=".Printers" />
        <activity android:name=".Inicio" />
        <activity android:name=".Registrar" />
        <activity android:name=".Ingresar" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>